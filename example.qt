Car = {
   doors = 4;
   maker = 'honda?';

   drive = {
      if(dist?){
         disp(`Drove`, dist?, 'miles!');
      } else {
         disp(`vroom vroom!`);
      }
   };
   __init = {
      __self?[maker] = new_maker?; // pre-process :P
   };
   __text = {
      "I'm a " + __self?.maker + ' with ' + (text(__self?.doors))$ + ' doors.'
   };
};
car = new Car?(new_maker = 'toyota');
disp( car?.__text{} );