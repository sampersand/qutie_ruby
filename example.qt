Car = {
   wheels = 4;
   maker = 'honda?';
   drive = { disp(`vroom vroom!`) };
   # init_old = {clone(c?)};
   init = {
      dup = (clone( Car? @ ()! ))$;
      dup? :V= (wheels, 5)!;
      dup?
   };
   1
};


# car_eval = Car? @ ()!, : init_old;
# init_func = car_eval? : init_old;
# inited = (init_func? @$ (c = car_eval?)!)$;
# disp(inited? : wheels)

inited = (Car? @ ()!, : init, @$ ()!)$;
disp(inited? : wheels)
disp(inited? )
disp(Car? @ ()!, : wheels)