disp? @ [b4: 0];

foo = {
   disp? @ [b4: 1];
   10
   ret_value = {
      disp? @ [b4: 2];
      20,30
      # return? @ (76)!;
      return? @ ( 40, 0)!;
      disp? @ [h{}i];
      disp? @ [l8: 2];
   } @$ ()!;
   disp? @ ('inner value: `', ret_value?, '`')!;
   disp? @ [l8: 1];
   50
};
disp? @ ('outer value: `', foo? @$ ()!, '`', )!;
# a=text? @ ('outer value: `', foo? @$ ()!, '`', )!;
# disp?@(---);
# a=(foo? @$ ()!)$;
# disp? @ ('outer value: `', a?, '`', )!;


disp? @ [l8: 0];
