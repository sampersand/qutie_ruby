Fraction <- {
  numer = numer? || __args?.0;
  denom = denom? || __args?.1;
  __text = { __self?.numer + '/' + __self?.denom };
  __num =  { __self?.numer    /    __self?.denom };
  __bool = { __self?.numer == 0 };
  __add_l = { Fraction? @ (numer? + right? * denom?, denom?)!

  };
  # __num = {
  #   __self?.wheels
  # };
  # __bool = { missing? }
};

frac = Fraction? @ (1, 3)!;
frac? + 3,
# text? @ (frac?)!,
# num? @ (frac?)!,
# bool? @ (frac?)!