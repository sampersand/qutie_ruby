Car = {
   DOORS = 4;
   drive = {
      disp(if(dist?, text('I drove', dist?, 'miles!'), `vroom vroom!`));
   };
   __init = {
      self.maker = new_maker? or 'honda';
   };
   __text = {
      text("I'm a", self.maker, 'with', self.DOORS, 'doors.')
   };
};
car = new Car(new_maker = 'toyota');
car = new Car(new_maker = 'toyota');

car?.drive(); #=> vroom vroom!
car?.drive(dist = 3;); #=> I drove 3 miles!
disp( car? ); #> I'm a toyota with 4 doors.


