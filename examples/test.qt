# a;

# foo = {bar={A?+b? }; bar?@(A=3); };
# bar={};
# foo?@(A=3;)!
a=3;
Current Speed = 65;
Speed Limit = 50;
Is Speeding = Current Speed? > Speed Limit?;
disp?@(Is Speeding? && "Slow down speed devil!" || "Speed up slow poke!")!;
