i = 0;
# until? @ (
#   (i? < 10)
#   {
#     disp? @ (i?)!;
#     i = i? + 1;
#   },
# )!;
unless? @ (i? > 10, (i=3))!,;
i?
# for? @ (
#   (i = 0;)
#   (i? < 10)
#   (i = i? + 1;)
#   { disp? @ (i?)!;});