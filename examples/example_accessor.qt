arr = [1, 2, 3]!; dup = (clone(arr?))$;

disp(arr?, dup?); arr? :S= (0, 3)!;
disp(arr?, dup?); disp('---');
map = {a=1; b=2; c=3;}!; dup = (clone(map?))$;
disp(map?, dup?); map? :V= (a, 3)!;
disp(map?, dup?); disp('---');

ambig = (1, 2, a=3;)!; dup = (clone(map?))$;

disp(ambig?, dup?); ambig? := (b, 4)!;
disp(ambig?, dup?); ambig? := (0, 5)!;
disp(ambig?, dup?); disp('---');
















