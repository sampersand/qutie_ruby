
print_data = function(a, b,) {
   
}
foo = {
   A = 1;
   disp('A?: ', A?or_, '\nB?: ', B?or_); # => 1, _
   {
      B = 2;
      disp('\tA?: ', A?or_, '\n\tB?: ', B?or_); # => 1, 2
      $?.-1, .= (B, 3)!;
      $?.-2, .= (B, 4)!;
      disp('\tA?: ', A?or_, '\n\tB?: ', B?or_); # => 1, 3
   }@()!;
   disp('A?: ', A?or_, '\nB?: ', B?or_); # => 1, 4
};
foo?@()!;
